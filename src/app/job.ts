export class Job {

    constructor(public id: String,
                totalBudget: number,
                totalSeats: number,
                division: string,
                salaryRange: number[],
                post: number) {
    }
}
